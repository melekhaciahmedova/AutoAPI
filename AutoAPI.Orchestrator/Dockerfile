# ---------------------------------------
# AutoAPI Builder - Stabil sürüm
# ---------------------------------------

FROM mcr.microsoft.com/dotnet/sdk:8.0

WORKDIR /src

# Bash varsa onu, yoksa sh'ı kullanan basit bekleme komutu
CMD ["sh", "-c", "echo '🛠 autoapi-builder ready with .NET SDK'; while true; do sleep 3600; done"]
